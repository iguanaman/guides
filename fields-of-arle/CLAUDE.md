Fields of Arle strategy guide — single-file `index.html`. Solo-only content throughout — no 2p-specific sections (e.g. tool-rush tempo, building-contest-as-interaction).

Action Board Reference (`#actionboard`) mirrors `feast-for-odin/index.html`'s action-board grid/modal pattern (`.board-grid`/`.action-card` CSS, click-to-expand modal) — sanctioned one-time exception to the "don't reference other guides' subfolders" rule; don't extend it to other sections.

Row order in the action board is fixed to the physical board's Summer/Winter pairing (1st Summer next to 1st Winter, etc.) — not resource-theme grouping. Don't reorder without checking `.research/Fields of Arle - Summary.pdf` p3 (gitignored — if missing, ask before reordering).

Anytime Actions (Load Vehicles, Rearrange Animals, Use Buildings, Use Peat Boat, Remove Equipment) render as a separate card grid below the seasonal board, not forced into the two-column grid.
